## 设计模式--文档案例

### 需求

#### 文档编辑器的功能

> 文件 编辑  样式 symbol
> 文档能够以不同风格自由混合文本和图形  下拉菜单 +滚动条 +跳到指定页的页码图标


### 思考 

#### 文档结构
1. 各种元素的组合 元素也有自己的结构
2. 目标: 
   - 保持文档的结构,
   - 可视化生成显示文档,
   - 根据不同的位置显示不同的文档内容


#### 组合
1. 将重要元素当成一个对象
   - 元素组成行
   - 多行成列，多列成页
#### Glyph '字形'

#### 用户操作

1. 创建
2. 打开 保存
3. 撤销 重做


### 封装一个请求

