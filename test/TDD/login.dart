import 'package:flutter_test/flutter_test.dart';
// import 'package:your_app/login_validator.dart'; // 假设存在 LoginValidator 类
//
// void main() {
//   group('LoginValidator', () {
//     test('用户名和密码正确时，应该返回登录成功', () {
//       final loginValidator = LoginValidator();
//       final result = loginValidator.validate('user', 'password123');
//       expect(result, '登录成功');
//     });
//
//     test('用户名错误时，应该返回用户名或密码错误', () {
//       final loginValidator = LoginValidator();
//       final result = loginValidator.validate('wrong_user', 'password123');
//       expect(result, '用户名或密码错误');
//     });
//
//     test('密码错误时，应该返回用户名或密码错误', () {
//       final loginValidator = LoginValidator();
//       final result = loginValidator.validate('user', 'wrong_password');
//       expect(result, '用户名或密码错误');
//     });
//
//     test('用户名为空时，应该返回用户名或密码不能为空', () {
//       final loginValidator = LoginValidator();
//       final result = loginValidator.validate('', 'password123');
//       expect(result, '用户名或密码不能为空');
//     });
//
//     test('密码为空时，应该返回用户名或密码不能为空', () {
//       final loginValidator = LoginValidator();
//       final result = loginValidator.validate('user', '');
//       expect(result, '用户名或密码不能为空');
//     });
//   });
// }
